# Analysis Notes

`Notebook.ipynb` is the polished report notebook for the portfolio.

The reproducible build logic lives in `scripts/build_analysis_assets.py`. It regenerates the cleaned analysis table, HIN output, exposure output, CEI-aware priority output, and chart assets from the full raw crash files plus committed reference layers.
